spring:
  application:
    name: networking1

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/real_estate
    username: root
    password: qkrrjsgnl#7

  jpa:
    hibernate:
      ddl-auto: update

logging:
    level:
      org:
        hibernate:
          type:
            descriptor:
              sql: trace

